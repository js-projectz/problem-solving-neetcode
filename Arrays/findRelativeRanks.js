/**
 * @param {number[]} score
 * @return {string[]}
 */
var findRelativeRanks = function (score) {

    // validation
    if (!score.length) return [];

    const map = new Map();



};

console.log(findRelativeRanks([10, 3, 8, 9, 4]));
